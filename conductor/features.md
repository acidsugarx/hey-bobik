# Bobik — Карта фич

## Текущие реализованные фичи

### 🎤 Голосовой ввод
- Wake word "Эй, Бобик" через Vosk (офлайн, grammar-constrained для низкого CPU)
- Свободная транскрипция голоса через Vosk (макс. 7 сек, silence detection 1 сек)
- Аудиозахват через PortAudio (16kHz, моно, буфер 4000 сэмплов)
- Non-blocking отправка аудио (дроп сэмплов при переполнении, защита от ALSA XRUNs)
- Drain буфера после каждой команды (защита от "призрачных" команд)

### 🧠 LLM-роутинг (Ollama)
- Локальный LLM (qwen3:8b) для intent-парсинга голосовых команд
- Формат роутинга: `ACTION: [NAME] | ARG: [VALUE]` — 7 действий
- Контекстная память (ring buffer, 10 последних команд) для связных диалогов
- Системный промпт на русском с примерами и правилами роутинга
- Отдельный Vision LLM (llava) для анализа скриншотов
- Не стриминг: ответ целиком (`stream: false`)

### 📝 NOTE — Заметки в Obsidian
- Создание daily note с YAML frontmatter (`date`, `source: Bobik`, `tags: [voice-note, inbox]`)
- Формат записи: `## HH:MM:SS\n{текст}\n\n`
- Имя файла: `{Prefix}{YYYY-MM-DD}.md` в Vault
- Обновление последней заметки (`UPDATE:`) — перезапись последнего `## ` блока
- Удаление последней заметки (действие CANCEL)

### ⏱ TIMER — Таймеры
- Запуск таймера по голосу ("поставь таймер на 5 минут" → 300 секунд)
- Несколько одновременных таймеров (именованные, карта с mutex)
- Автозамена таймера с тем же именем
- Уведомление через notify-send при срабатывании ("Время вышло: {name}")
- Отмена всех таймеров (действие CANCEL)

### 🕐 TIME — Текущее время
- Ответ на "сколько времени?" → формат HH:MM (24-часовой)
- Injectable `Now` для тестируемости

### 📋 CLIPBOARD — Буфер обмена
- Чтение буфера (`xclip` / `xsel` / `wl-paste` — автодетект)
- Запись текста в буфер
- Запись содержимого буфера в заметку Obsidian
- Поддержка X11 и Wayland

### 🧮 CALC — Калькулятор
- Безопасное вычисление через Go AST парсер (никакого произвольного кода)
- Операции: `+`, `-`, `*`, `/`, `%` (modulo), скобки, унарный `-`/`+`
- Процентные вычисления ("15% от 2500" → `15%:2500` → 375)
- Конвертация русской запятой (`,` → `.`)
- Умное форматирование (целые без дробей, float с 2 знаками)

### 🖥 SCREEN — Анализ экрана
- Скриншот всего экрана (describe, read)
- Скриншот активного окна (window)
- Бэкенды: `gnome-screenshot` → `scrot` → `grim` → `spectacle` → `maim` (автодетект)
- Vision LLM анализ (llava): описание, чтение текста, анализ окна
- Автоочистка временных файлов
- `CaptureArea()` реализован, но НЕ подключён к оркестратору (мёртвый код)

### 🔊 TTS — Озвучка
- Text-to-speech через `espeak-ng` (голос `ru`) или `piper` (модель `ru_RU-dmitri-medium`)
- Асинхронный (`SpeakAsync` в горутине)
- Выключен по умолчанию (privacy-first)
- Фидбэк: "Записал", "Таймер запущен", "Сейчас HH:MM", "Отменено" и т.д.

### 🔔 Уведомления
- `notify-send` для всех событий жизненного цикла
- Шаблон: "Бобик: {заголовок}" → {описание}

### 🖥 System Tray
- Иконка 64×64 PNG, программно генерируемая
- 3 состояния: серый (idle), зелёный (listening), синий (thinking)
- Меню: только "Quit"
- Работает на главном потоке (GTK требование)

### ⚙️ Конфигурация
- JSON файл + env overrides (`BOBIK_*`) + CLI флаги
- Каскадный поиск: `--config` → `~/.config/bobik/config.json` → `~/.bobik.json` → `./bobik.json`
- 17 параметров (audio, STT, LLM, vision, obsidian, TTS, logging)

---

## Нереализованные / потенциальные фичи

### 🔥 Высокий приоритет (natural extensions)

- **Стриминг LLM ответов** — сейчас `stream: false`, ожидание полного ответа. Стриминг даст мгновенный фидбэк TTS/UI
- **Произвольный диалог (chat mode)** — сейчас Бобик только роутит в 7 инструментов. Нет возможности просто поболтать или задать вопрос ("кто такой Пушкин?")
- **RAG по заметкам** — поиск по Obsidian vault, ответы на вопросы по контексту заметок
- **Редактирование конкретной заметки** — сейчас UPDATE только для последней. Нужно: "исправь заметку про молоко"
- **Умный парсинг времени** — "через полчаса", "в 15:30", "завтра в 8" → парсинг натурального языка для таймеров/напоминаний
- **Персистентные напоминания** — таймеры теряются при перезапуске. Нужно сохранение на диск
- **Многоязычность STT** — сейчас только русский. Добавить английский (или auto-detect)

### 📱 Средний приоритет (расширение возможностей)

- **Tool-use / Function calling** — нативный function calling LLM вместо парсинга `ACTION: | ARG:` регулярками
- **MCP (Model Context Protocol)** — подключение к экосистеме MCP серверов (файлы, git, веб, API)
- **Плагинная система** — динамическая загрузка инструментов (Go plugins или gRPC)
- **Цепочки действий (multi-step)** — "запиши заметку и поставь напоминание через час" (сейчас: одно действие за раз)
- **Telegram/Slack интеграция** — отправка заметок, уведомлений, управление ботом удалённо
- **Веб-интерфейс** — история команд, настройки, просмотр заметок
- **Горячие клавиши** — активация без wake word (глобальный хоткей)
- **Выделение области экрана** — `CaptureArea()` уже есть, нужно подключить к оркестратору
- **OCR без Vision LLM** — Tesseract для быстрого чтения текста с экрана (не требует GPU)

### 🌟 Низкий приоритет (cool but complex)

- **Whisper STT** — замена Vosk на faster-whisper для лучшего качества транскрипции
- **Wake word через нейросеть** — OpenWakeWord / Porcupine вместо grammar-constrained Vosk
- **Голосовая идентификация** — распознавание пользователя (speaker diarization)
- **Smart home интеграция** — Home Assistant API (свет, розетки, датчики)
- **Музыка/медиа** — управление MPRIS (play/pause/next), Spotify
- **Системные команды** — "открой Firefox", "выключи компьютер" (строго по whitelist, не произвольный shell)
- **Заметки с тегами** — автоматическая классификация заметок через LLM
- **Суммаризация дня** — "что я записал сегодня?" → LLM обработка daily note
- **Контекст из активного окна** — автоматическое добавление контекста ("в каком приложении я сейчас?")
- **Режим диктовки** — непрерывная транскрипция → вставка текста в активное поле ввода
- **Локальный embeddings** — векторный поиск по заметкам (через Ollama embeddings)
- **Расписание** — интеграция с Google Calendar / CalDAV
- **Мультимодальная память** — запомни скриншот + голосовую заметку + контекст окна = rich entry

---

## Архитектурные ограничения текущей реализации (Go)

| Проблема | Описание |
|----------|----------|
| **Нет стриминга** | `net/http` + JSON — нет нативной поддержки SSE/streaming для LLM |
| **Нет async/await** | Горутины + каналы работают, но сложные async pipelines (LLM chain, multi-step) неудобны |
| **Нет нативного function calling** | Парсинг `ACTION: \| ARG:` регулярками — хрупко. Нет JSON schema validation |
| **Нет горячей перезагрузки плагинов** | Go plugins (`plugin.Open`) ограничены и ненадёжны |
| **Ограниченная экосистема AI/ML** | Нет аналогов LangChain, LlamaIndex, CrewAI. Всё с нуля |
| **CGo зависимости** | Vosk и PortAudio требуют CGo — усложняет кросс-компиляцию и CI |
| **Нет REPL / hot reload** | Каждое изменение = перекомпиляция |
